package routers

import (
	"github.com/gin-gonic/gin"
)

func SetupUserRouter(Router *gin.RouterGroup) {

	// Initialize routers

	/*userRoutes := Router.Group("/auth")
	{
		userRoutes.POST("register", controllers.Register)
		userRoutes.POST("", controllers.CreateUserHandler)
		userRoutes.GET("/:id", controllers.GetUserByIDHandler)
		userRoutes.PUT("/:id", controllers.UpdateUserHandler)
		userRoutes.DELETE("/:id", controllers.DeleteUserHandler)
	}*/

}
